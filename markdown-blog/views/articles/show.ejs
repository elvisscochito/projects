<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <h1 class="mt-4 mb-4"><%= article.title %></h1>
        <div class="mb-2 text-muted">
            <%= article.date.toLocaleDateString() %>
        </div>
        <div class="mb-4">
            <a href="/home" class="btn btn-secondary">All articles</a>
            <!-- go to router.get('/edit/:id' route -->
            <a href="/articles/edit/<%= article.id %>" class="btn btn-info text-light">Edit</a>
        </div>

        <div>
            <!-- from <%= article.markdown %> to actual <%- article.sanitizedHTML %> database content variable, the - is fow raw html instead of string it -->
            <%- article.sanitizedHTML %>
        </div>
    </div>  
</body>
</html>
